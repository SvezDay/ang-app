export class Course {
   // id = '';
   // name = '';
   // id: number;
   // name: string;
   //
   // constructor(values: Object = {}) {
   //  Object.assign(this, values);
   // }
   constructor(
      public id: number,
      public name: string
   ){}
}
